# Netlify redirects for SPA deployed at root domain
# Handle all client-side routes by serving index.html
/*    /index.html   200

# Legacy support for any existing /asia50/ paths (optional)
/asia50/* /index.html 200
